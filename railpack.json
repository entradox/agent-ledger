{
  "build": {"installCommand": "pip install -r requirements.txt pydantic-settings"},
  "deploy": {"startCommand": "python3 -m uvicorn api_server:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips=\"*\""}
}