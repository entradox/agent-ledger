{
 "project": "agent-ledger",
 "updated": "2026-09-13",
 "features": [
  {
   "name": "per-agent spend ledger",
   "status": "shipped"
  },
  {
   "name": "MCP + REST + CLI parity",
   "status": "shipped"
  },
  {
   "name": "idempotency + typed errors + version gate",
   "status": "shipped"
  },
  {
   "name": "scarcity window (first 50 free)",
   "status": "shipped",
   "note": "re-scoped to workspaces, this phase; grant expires after 1 year via pro_until"
  },
  {
   "name": "credential-gated reads (REST + MCP)",
   "status": "shipped",
   "note": "agent_secret / workspace_key / dashboard session, all via identity.authorize_agent_access"
  },
  {
   "name": "owner dashboard",
   "status": "shipped",
   "note": "TWO surfaces: the admin one (/v1/dashboard, X-Al-Admin gated, pre-dates workspace identity) and the workspace one (/dashboard, D-1226 — key-paste, same-origin JS, CSP). The workspace surface is what a customer can actually use."
  },
  {
   "name": "per-agent human-readable report page",
   "status": "shipped",
   "note": "The authenticated /v1/report/{id}/html needs a credential header a browser cannot send; the browser-usable path is a share link (D-1217, POST /v1/report/{id}/share — read-only, expiring, revocable) or the /dashboard workspace surface (D-1226)."
  },
  {
   "name": "workspace identity core",
   "status": "shipped",
   "note": "Tasks 1-4 of docs/superpowers/plans/2026-09-10-workspace-identity.md"
  },
  {
   "name": "google oauth + session dashboard",
   "status": "shipped",
   "note": "Task 5"
  },
  {
   "name": "per-workspace stripe billing",
   "status": "shipped",
   "note": "Task 6"
  },
  {
   "name": "x402 self-serve workspace minting",
   "status": "shipped",
   "note": "Task 7; tx_hash idempotency via ledger_engine's idempotency store"
  },
  {
   "name": "existing-agent migration to workspaces",
   "status": "planned",
   "note": "Task 9; script takes --google-sub so the migrated workspace matches the operator's real login"
  },
  {
   "name": "database migration (flat files -> real DB)",
   "status": "not_started",
   "note": "deferred per spec 3d, triggered by usage"
  },
  {
   "name": "workspace key rotation/revocation UI",
   "status": "not_started",
   "note": "workspace_engine.reissue_key() exists as the building block; deliberately not wired to any automatic path"
  },
  {
   "name": "custom domain — aiagentscity.com live with TLS",
   "status": "shipped",
   "note": "ab17965 (BUG-5). Both hosts serve 200 with valid certs; Porkbun ALIAS apex + CNAME www to the Railway service, MX/SPF/NS untouched so email forwarding survives. 44 infrastructure-host references replaced across client, manifests, docs and onboarding email; a test now fails if any public surface mentions up.railway.app. Railway host still SERVES (health checks) but is advertised nowhere."
  },
  {
   "name": "cache-tier pricing (Claude) — input is three prices, not one",
   "status": "shipped",
   "note": "8b12a93. Anthropic bills input at 1x fresh / 1.25x 5m cache write / 2x 1h cache write / 0.1x cache read. Priced against a real Claude Code session: $15.22 true vs $112.85 at the flat rate (7.4x). 13 Claude entries now verified:true from Anthropic's pricing page; OpenAI entries stay false and say so. 15 tests."
  },
  {
   "name": "Claude Code skill: MCP + automatic session reporting",
   "status": "shipped",
   "note": "8b12a93 (BUILD-6). integrations/claude-code/: report-session.py parses the transcript (deduped by message id — 317 records for 181 messages in one real session, so summing would overstate ~75%), install.py merges MCP + a Stop hook into Claude Code settings without rewriting them. 17 reporter tests + 10 installer tests."
  },
  {
   "name": "SDK wrappers: one line to the proxy (agentledger.wrap)",
   "status": "shipped",
   "note": "BUILD-3 (06faf27). Repoints an existing OpenAI/Anthropic SDK client at the proxy without patching or subclassing it; the provider credential is untouched. Tested against the REAL installed SDKs - which is how the httpx-vs-httpx2 URL-type incompatibility was found. 8 tests."
  },
  {
   "name": "CLI init + share + client packaging",
   "status": "shipped",
   "note": "BUILD-5 (06faf27). `agent-ledger init` mints a workspace, claims an agent, writes a working .env; `share` prints a browser-openable link. pyproject ships the CLIENT only (agentledger + cli); the engine stays server-side. NOT published to PyPI - installs from the repo or git."
  },
  {
   "name": "provider coverage: any vendor, config not code (DeepSeek)",
   "status": "shipped",
   "note": "fd5cb57. providers.json makes the proxy forward to any OpenAI/Anthropic-compatible vendor; pricing is keyed by MODEL so a caller cannot route around its price by switching endpoints; cache-hit input is priced at the cache rate (DeepSeek hit rate ~50x cheaper). Live: /v1/pricing shows 11 models incl. deepseek-v4-flash with cache_hit; the 404 lists a config-only provider (moonshot) proving nothing in the code knows it. Prices marked verified:false - read from a docs page, not confirmed by an operator."
  },
  {
   "name": "token-to-cost auto-pricing on /v1/track",
   "status": "shipped",
   "note": "BUILD-4 (7dbc18d). amount_cents optional: tokens+model are priced from the shared table; an unpriced model is refused (422 model_not_priced) rather than recorded as free. Live-verified fail-closed paths; happy path covered by 9 tests."
  },
  {
   "name": "proxy enforcement (block the spend, not the write)",
   "status": "shipped",
   "note": "D-1222 (d741a87). POST /proxy/{provider}/{path} blocks before the provider is contacted; pass-through, no key custody. Route live; the block-before-provider property is proven against a fake upstream in tests, not yet live. model_prices.json is UNVERIFIED placeholders."
  },
  {
   "name": "trust surface: honest claims, privacy/terms, real status page",
   "status": "shipped",
   "note": "D-1219 (6f92ea7). Verified live: no 'real enforcement' claim, data-handling statement published and asserted against the data model, /privacy + /terms + a real /status health page. LEGAL WORDING NEEDS A MORGAN PASS."
  },
  {
   "name": "shareable report links (read-only, expiring, revocable)",
   "status": "shipped",
   "note": "D-1217 (080fc40). Verified live 2026-09-13: mint 200, opens with zero credentials as text/html, tampered 403 friendly page, cross-agent 403."
  },
  {
   "name": "alert delivery (webhook + email, with receipts)",
   "status": "shipped",
   "note": "D-1218 (0d10eb9/870384e). Routes live and fail-closed in prod. Delivery proven against a local HTTP sink in tests; not yet exercised in production because the operator holds no workspace_key."
  },
  {
   "name": "agent-secret rotation/revocation (owner recovery)",
   "status": "shipped",
   "note": "D-1216 (f7849d2) — LIVE in prod, verified 2026-09-13 by probing the MCP tools ledger_rotate_secret / ledger_revoke_secret with bogus credentials (correct fail-closed: workspace_key_required). Previous 'planned' note was a record conflict: the commit is an ancestor of main and the tools serve. Workspace_key-only by design (an agent's own secret cannot rotate itself)."
  },
  {
   "name": "per-workspace rate limiting",
   "status": "not_started"
  },
  {
   "name": "mcp.so / Smithery / PulseMCP directory listings",
   "status": "not_started"
  },
  {
   "name": "launch post",
   "status": "not_started",
   "note": "never auto-published, needs explicit approval"
  },
  {
   "name": "human buy path (front door + /start + reference-tagged checkout)",
   "status": "shipped",
   "note": "shipped D-1162. GET / renders the product page; POST /start mints a FREE-tier workspace, shows the key once (Cache-Control no-store), 3 mints/IP/day; the upgrade URL carries client_reference_id so the webhook upgrades that workspace. Verified live end-to-end with a signed checkout.session.completed: record went plan free/agent_cap 3 -> plan pro/agent_cap null, customers.jsonl row written"
  },
  {
   "name": "google auth removed",
   "status": "shipped",
   "note": "shipped D-1162. routes_auth.py, oauth_google.py, session_auth.py deleted; the four routes 404; session_cookie removed from identity.authorize_agent_access; verified absent from the deployed image"
  }
 ]
}